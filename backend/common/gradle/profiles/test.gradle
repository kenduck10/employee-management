apply from: 'gradle/db.gradle'

ext.getDatasource = {
    return createDatasource('my-properties-test.yml')
}

ext.getFlywayParam = {
    def datasource = getDatasource()
    return createFlywayParam(datasource, null)
}
