apply from: 'gradle/db.gradle'

ext.getDatasource = {
    return createDatasource('my-properties.yml')
}

ext.getFlywayParam = {
    def datasource = getDatasource()
    return createFlywayParam(datasource, "filesystem:${resourcesPath}/db/migration/local")
}
